import { SetClassDirective } from './set-class.directive';

describe('SetClassDirective', () => {
  it('should create an instance', () => {
    const directive = new SetClassDirective();
    expect(directive).toBeTruthy();
  });
});
